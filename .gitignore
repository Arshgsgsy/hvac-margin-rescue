.DS_Store

# Local backup created while flattening nested repositories
*.git.backup*

# GitHub hard limit workaround: keep the raw labor log local and track
# the compressed copy in the repo instead.
Hack Data/labor_logs_all.csv
